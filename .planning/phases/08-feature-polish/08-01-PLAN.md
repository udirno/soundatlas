---
phase: 08-feature-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/components/AIChatPanel.tsx
autonomous: true

must_haves:
  truths:
    - "Chat panel header shows an expand button that opens the panel to a larger/fullscreen view"
    - "Expanded chat view shows a collapse button that returns it to the default sidebar size"
    - "Expand/collapse toggle preserves message history and input state"
  artifacts:
    - path: "frontend/src/components/AIChatPanel.tsx"
      provides: "Expandable chat panel with toggle between sidebar and fullscreen modes"
      contains: "isExpanded"
  key_links:
    - from: "AIChatPanel expand button"
      to: "isExpanded state"
      via: "onClick toggles boolean, conditional classes change dimensions"
      pattern: "isExpanded.*Maximize|Minimize"
---

<objective>
Add expand/collapse functionality to the AI chat panel so users can toggle between the current compact sidebar (w-96 h-[580px]) and a larger near-fullscreen view for comfortable reading of long AI responses.

Purpose: Long AI responses are hard to read in the small sidebar panel. An expand control gives users room to read comfortably, and a collapse control lets them return to the compact view.
Output: Updated AIChatPanel.tsx with expand/collapse toggle.
</objective>

<execution_context>
@/Users/udirno/.claude/get-shit-done/workflows/execute-plan.md
@/Users/udirno/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@frontend/src/components/AIChatPanel.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add expand/collapse toggle to AIChatPanel</name>
  <files>frontend/src/components/AIChatPanel.tsx</files>
  <action>
    1. Import `Maximize2` and `Minimize2` icons from lucide-react (alongside existing `X` import).

    2. Add state: `const [isExpanded, setIsExpanded] = useState(false);`

    3. In the header bar (between the "Ask AI" title and the X close button), add an expand/collapse toggle button:
       - When collapsed: show `Maximize2` icon (size 14), aria-label="Expand chat"
       - When expanded: show `Minimize2` icon (size 14), aria-label="Collapse chat"
       - onClick: `() => setIsExpanded(prev => !prev)`
       - Style: same as the existing close button (`text-gray-400 hover:text-white transition-colors p-1 rounded`)
       - Place both buttons (expand toggle + close X) in a flex row with gap-1

    4. Replace the static outer div classes with conditional classes based on `isExpanded`:
       - Collapsed (default, current): `fixed bottom-4 right-4 w-96 h-[580px] z-50`
       - Expanded: `fixed inset-4 z-50` (this gives a near-fullscreen overlay with 1rem margin on all sides)
       - Shared classes (both states): `flex flex-col rounded-xl shadow-2xl border border-gray-700 overflow-hidden bg-gray-900 text-white`

    5. Add smooth transition: include `transition-all duration-200` on the outer div so the resize animates.

    6. Do NOT change any other behavior -- messages, input, suggestions, loading state all stay the same.
  </action>
  <verify>
    Run `cd /Users/udirno/Desktop/SoundAtlas/frontend && npx tsc --noEmit` to confirm no type errors.
    Visually confirm in the code: isExpanded state exists, Maximize2/Minimize2 imports present, conditional classes on outer div.
  </verify>
  <done>
    - Chat panel renders at default size (w-96 h-[580px]) when isExpanded is false
    - Clicking the expand button sets isExpanded to true and panel fills the viewport (inset-4)
    - Clicking the collapse button sets isExpanded to false and panel returns to default size
    - Close (X) button still works in both states
    - No type errors
  </done>
</task>

</tasks>

<verification>
1. `cd /Users/udirno/Desktop/SoundAtlas/frontend && npx tsc --noEmit` -- no errors
2. AIChatPanel.tsx contains `isExpanded` state variable
3. AIChatPanel.tsx imports `Maximize2` and `Minimize2` from lucide-react
4. Outer div uses conditional classes switching between `bottom-4 right-4 w-96 h-[580px]` and `inset-4`
5. Header has two buttons: expand/collapse toggle and close (X)
</verification>

<success_criteria>
- CHAT-01: Chat panel has an expand control (Maximize2 icon) that opens it to near-fullscreen (inset-4)
- CHAT-02: Expanded view has a collapse control (Minimize2 icon) that returns it to sidebar size (w-96 h-[580px])
- No regressions: close button, messages, input, suggestions all work unchanged
</success_criteria>

<output>
After completion, create `.planning/phases/08-feature-polish/08-01-SUMMARY.md`
</output>
