---
phase: 05-global-stats-and-search
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/app/schemas/search.py
  - backend/app/services/search_service.py
  - frontend/src/lib/api.ts
  - frontend/src/components/StatsSidebar.tsx
  - frontend/src/components/HomeClient.tsx
autonomous: true

must_haves:
  truths:
    - "A sidebar panel displays total countries, total unique artists, total tracks, top genre, and diversity score on a 0-10 scale"
    - "The sidebar shows the top 5 countries ranked by artist count"
    - "Top 5 countries in sidebar are clickable and navigate the map"
    - "Backend search endpoint returns country_id for both artist and track hits"
  artifacts:
    - path: "frontend/src/components/StatsSidebar.tsx"
      provides: "Global analytics sidebar panel"
      min_lines: 60
    - path: "frontend/src/lib/api.ts"
      provides: "fetchDashboard, fetchSearch functions and DashboardStats, SearchResult types"
      exports: ["fetchDashboard", "fetchSearch", "DashboardStats", "SearchResult"]
    - path: "backend/app/schemas/search.py"
      provides: "SearchArtistHit and SearchTrackHit with country_id field"
      contains: "country_id"
    - path: "backend/app/services/search_service.py"
      provides: "Artist.country_id in artist query, Artist.country_id via join in track query"
      contains: "Artist.country_id"
  key_links:
    - from: "frontend/src/components/StatsSidebar.tsx"
      to: "/api/analytics/dashboard"
      via: "fetchDashboard in useEffect on mount"
      pattern: "fetchDashboard"
    - from: "frontend/src/components/StatsSidebar.tsx"
      to: "frontend/src/components/HomeClient.tsx"
      via: "onCountrySelect callback prop"
      pattern: "onCountrySelect"
    - from: "frontend/src/components/HomeClient.tsx"
      to: "frontend/src/components/StatsSidebar.tsx"
      via: "renders StatsSidebar with onCountrySelect"
      pattern: "<StatsSidebar"
---

<objective>
Build the global stats sidebar and extend the backend search schema to include country_id. This plan delivers the analytics sidebar (STAT-01, STAT-02, STAT-03), adds frontend API client types/functions for both dashboard and search endpoints, fixes the critical backend gap (search hits missing country_id), and wires the sidebar into HomeClient with clickable top countries.

Purpose: The sidebar is the user's at-a-glance view of their library's geographic diversity. The backend search fix unblocks Plan 05-02's map navigation on search selection.
Output: StatsSidebar component rendering live stats, updated api.ts with all Phase 5 types/functions, backend search returning country_id.
</objective>

<execution_context>
@/Users/udirno/.claude/get-shit-done/workflows/execute-plan.md
@/Users/udirno/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-global-stats-and-search/05-RESEARCH.md
@frontend/src/lib/api.ts
@frontend/src/components/HomeClient.tsx
@backend/app/schemas/search.py
@backend/app/services/search_service.py
@backend/app/services/analytics_service.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend backend search schema and service to include country_id</name>
  <files>backend/app/schemas/search.py, backend/app/services/search_service.py</files>
  <action>
  1. In `backend/app/schemas/search.py`:
     - Add `country_id: Optional[int] = None` to `SearchArtistHit` (after `image_url`)
     - Add `country_id: Optional[int] = None` to `SearchTrackHit` (after `album_name`)

  2. In `backend/app/services/search_service.py`:
     - In the artist query (`artist_stmt`): Add `Artist.country_id` to the `select()` columns (before the similarity label)
     - In the track query (`track_stmt`): Add a LEFT JOIN from Track to Artist (`Track.artist_id == Artist.id`, `isouter=True`) and add `Artist.country_id` to the `select()` columns. The `isouter=True` is needed because `artist_id` on tracks could theoretically be null.
     - Add `from app.models.artist import Artist` is already imported — just confirm it is present for the track query join.

  Use `@router.get("")` convention (empty string, not "/") per established project decisions.
  </action>
  <verify>
  Run inside Docker:
  ```bash
  docker compose exec backend python -c "
  from app.schemas.search import SearchArtistHit, SearchTrackHit
  a = SearchArtistHit(id=1, name='Test', score=0.5, country_id=42)
  t = SearchTrackHit(id=1, name='Test', score=0.5, in_library=True, country_id=7)
  print(f'Artist country_id: {a.country_id}')
  print(f'Track country_id: {t.country_id}')
  print('Schema validation passed')
  "
  ```
  Then test the search endpoint:
  ```bash
  curl -s "http://localhost:8000/api/search?q=rihanna" | python3 -m json.tool | head -20
  ```
  Verify `country_id` field appears in artist and track results.
  </verify>
  <done>SearchArtistHit and SearchTrackHit both have country_id fields. The search endpoint returns country_id for artist hits (directly from Artist.country_id) and for track hits (via Artist join). Null country_id is handled gracefully.</done>
</task>

<task type="auto">
  <name>Task 2: Add frontend API types/functions and build StatsSidebar with HomeClient wiring</name>
  <files>frontend/src/lib/api.ts, frontend/src/components/StatsSidebar.tsx, frontend/src/components/HomeClient.tsx</files>
  <action>
  1. In `frontend/src/lib/api.ts`, add these types and functions after the existing exports:

     Types to add:
     - `DashboardStats`: `{ country_count: number; artist_count: number; track_count: number; diversity_score: number; top_genres: Array<{ genre: string; count: number }>; top_countries: Array<{ id: number; name: string; iso_alpha2: string; artist_count: number }> }`
     - `SearchArtistHit`: `{ id: number; name: string; spotify_id: string | null; genres: string[] | null; image_url: string | null; score: number; country_id: number | null }`
     - `SearchTrackHit`: `{ id: number; name: string; spotify_id: string | null; album_name: string | null; score: number; in_library: boolean; country_id: number | null }`
     - `SearchResult`: `{ query: string; artists: SearchArtistHit[]; tracks: SearchTrackHit[] }`

     Functions to add (follow existing fetchCountries pattern):
     - `fetchDashboard()`: GET `${getBaseUrl()}/api/analytics/dashboard`, returns `Promise<DashboardStats>`
     - `fetchSearch(q: string)`: GET `${getBaseUrl()}/api/search?q=${encodeURIComponent(q)}`, returns `Promise<SearchResult>`

  2. Create `frontend/src/components/StatsSidebar.tsx`:
     - Fixed position on the left side: `fixed top-0 left-0 h-screen w-72 bg-gray-950/95 backdrop-blur-sm border-r border-gray-800 z-40 overflow-y-auto`
     - Fetch dashboard stats on mount using `fetchDashboard()` in a `useEffect` (same pattern as CountryPanel)
     - Loading state: show a subtle skeleton/spinner while loading
     - Display sections:
       a. **Header**: "Library Stats" or "Your Library" title
       b. **Key Metrics** (grid or stacked): Total countries, Total artists, Total tracks
       c. **Top Genre**: Show the first item from `top_genres` array
       d. **Diversity Score**: Display `(diversity_score * 10).toFixed(1)` out of 10. Include a small visual indicator (e.g., a thin progress bar or colored text based on score level)
       e. **Top 5 Countries**: Show `top_countries.slice(0, 5)` as a ranked list. Each row shows rank number, country name, and artist count. Make each row clickable — on click, call `onCountrySelect(country.id)` prop.
     - Props: `onCountrySelect: (countryId: number) => void`
     - Use Tailwind for all styling. Dark theme consistent with the existing map aesthetic (gray-950 backgrounds, gray-400 text, white headings).
     - Handle error state: if fetch fails, show "Stats unavailable" message (do not crash).

  3. In `frontend/src/components/HomeClient.tsx`:
     - Import `StatsSidebar` from `./StatsSidebar`
     - Add `<StatsSidebar onCountrySelect={setSelectedCountryId} />` inside the `<main>` element, before `MapView`
     - The sidebar is always visible (not toggled)

  Note: Do NOT add search or flyToTarget changes to HomeClient or MapView yet — that is Plan 05-02.
  </action>
  <verify>
  ```bash
  cd /Users/udirno/Desktop/SoundAtlas/frontend && npx tsc --noEmit
  ```
  Then visit `http://localhost:3000` in a browser and confirm:
  - The sidebar appears on the left side with stats loaded from the API
  - Total countries, artists, tracks are displayed
  - Top genre is shown
  - Diversity score is displayed on a 0-10 scale
  - Top 5 countries are listed with artist counts
  - Clicking a top country opens the CountryPanel on the right
  </verify>
  <done>StatsSidebar renders on the left side with all global analytics (country count, artist count, track count, top genre, diversity score on 0-10 scale, top 5 countries ranked by artist count). Clicking a top country sets selectedCountryId and opens CountryPanel. api.ts exports all Phase 5 types and fetch functions.</done>
</task>

</tasks>

<verification>
1. Backend: `curl "http://localhost:8000/api/search?q=rihanna"` returns results with `country_id` field on both artist and track hits
2. Backend: `curl "http://localhost:8000/api/analytics/dashboard"` returns stats (this already works, just confirming)
3. Frontend: TypeScript compiles without errors (`npx tsc --noEmit`)
4. Frontend: StatsSidebar visible on left side at localhost:3000 with all required stats
5. Frontend: Clicking a top-5 country in sidebar opens CountryPanel on the right
</verification>

<success_criteria>
- STAT-01: Sidebar shows total countries, total artists, total tracks
- STAT-02: Sidebar shows top genre and diversity score on 0-10 scale
- STAT-03: Sidebar shows top 5 countries ranked by artist count
- Backend search results include country_id for both artists and tracks (unblocks SRCH-02)
- api.ts exports DashboardStats, SearchResult, SearchArtistHit, SearchTrackHit types and fetchDashboard, fetchSearch functions
</success_criteria>

<output>
After completion, create `.planning/phases/05-global-stats-and-search/05-01-SUMMARY.md`
</output>
