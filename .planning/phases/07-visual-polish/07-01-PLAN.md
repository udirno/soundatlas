---
phase: 07-visual-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/components/StatsSidebar.tsx
  - frontend/src/components/GenrePieChart.tsx
  - frontend/src/components/CountryPanel.tsx
  - frontend/src/components/MapView.tsx
  - frontend/src/components/SearchBar.tsx
autonomous: true

must_haves:
  truths:
    - "StatsSidebar header has one consistent text size (text-xs) and valid tracking class"
    - "Pie chart labels do not overlap — slices under 5% show no label"
    - "Top tracks never show album name that duplicates the track name"
    - "Map tooltip shows country name on its own styled line, metadata on a second line"
    - "SearchBar input shows a subtle focus ring when focused"
  artifacts:
    - path: "frontend/src/components/StatsSidebar.tsx"
      provides: "Fixed h2 section header with non-conflicting classes"
      contains: "text-xs font-semibold text-gray-400 uppercase tracking-wider"
    - path: "frontend/src/components/GenrePieChart.tsx"
      provides: "Custom label render function with percent threshold"
      contains: "MIN_LABEL_PERCENT"
    - path: "frontend/src/components/CountryPanel.tsx"
      provides: "Top tracks deduplication guard"
      contains: "track.album_name !== track.name"
    - path: "frontend/src/components/MapView.tsx"
      provides: "Improved tooltip HTML with visual hierarchy"
      contains: "font-weight:600"
    - path: "frontend/src/components/SearchBar.tsx"
      provides: "Focus ring on search input"
      contains: "focus:ring"
  key_links:
    - from: "frontend/src/components/GenrePieChart.tsx"
      to: "recharts Pie component"
      via: "label render prop"
      pattern: "label=\\{renderLabel\\}"
---

<objective>
Apply visual polish to all UI panels: fix class conflicts, suppress overlapping pie labels, add top-tracks deduplication guard, improve map tooltip hierarchy, and add search input focus ring.

Purpose: Satisfy all 8 UI polish requirements (UI-01 through UI-08) for v1.1 visual consistency.
Output: Five updated component files with refined visual quality.
</objective>

<execution_context>
@/Users/udirno/.claude/get-shit-done/workflows/execute-plan.md
@/Users/udirno/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-visual-polish/07-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix StatsSidebar header, pie chart labels, and top tracks dedup</name>
  <files>
    frontend/src/components/StatsSidebar.tsx
    frontend/src/components/GenrePieChart.tsx
    frontend/src/components/CountryPanel.tsx
  </files>
  <action>
    **StatsSidebar.tsx (UI-02):**
    Line 41 — replace the entire h2 className string. Current value has conflicting text-base + text-xs and invalid letter-spacing-wider class. Replace with:
    `text-xs font-semibold text-gray-400 uppercase tracking-wider mb-5`
    This matches the canonical section header pattern used in CountryPanel and elsewhere in StatsSidebar itself.

    **GenrePieChart.tsx (UI-04):**
    1. Add a `renderLabel` function above the component, outside the export. The function signature is `(props: PieLabelRenderProps) => ReactNode | null`.
    2. Define constants: `const RADIAN = Math.PI / 180;` and `const MIN_LABEL_PERCENT = 0.05;`
    3. In renderLabel: destructure `{ cx, cy, midAngle, outerRadius, name, percent }` from props. If `(percent as number) < MIN_LABEL_PERCENT`, return `null`. Otherwise, compute label position at `outerRadius + 20` radius using cos/sin, and return an SVG `<text>` element with `fill="#9ca3af"`, `fontSize={11}`, `dominantBaseline="central"`, and `textAnchor` based on whether `x > cx` ("start" or "end"). Text content is `{name as string}`.
    4. Replace the inline arrow function in the `<Pie label={...}>` prop with `label={renderLabel}`.
    5. Keep `labelLine={false}` as-is.
    6. The `PieLabelRenderProps` import already exists on line 9 — no new imports needed.

    **CountryPanel.tsx (UI-08):**
    Line 244 — change the guard from `{track.album_name && (` to `{track.album_name && track.album_name !== track.name && (`. This prevents displaying album name when it duplicates the track name (common for singles/EPs from Spotify data).
  </action>
  <verify>
    Run `cd /Users/udirno/Desktop/SoundAtlas/frontend && npx tsc --noEmit` — no type errors.
    Grep StatsSidebar.tsx for "letter-spacing-wider" — should return 0 matches.
    Grep StatsSidebar.tsx for "text-base" — should return 0 matches (on the h2 line).
    Grep GenrePieChart.tsx for "MIN_LABEL_PERCENT" — should return a match.
    Grep CountryPanel.tsx for "track.album_name !== track.name" — should return a match.
  </verify>
  <done>
    StatsSidebar h2 has exactly one text-size class (text-xs) and uses tracking-wider (not letter-spacing-wider). GenrePieChart suppresses labels on pie slices under 5% via a custom render function. CountryPanel top tracks guard prevents album_name === track.name display.
  </done>
</task>

<task type="auto">
  <name>Task 2: Polish map tooltip and search input focus</name>
  <files>
    frontend/src/components/MapView.tsx
    frontend/src/components/SearchBar.tsx
  </files>
  <action>
    **MapView.tsx (UI-06):**
    Line 120 — replace the tooltip HTML template literal. Current: `<strong>${props.name}</strong><br/>${props.artist_count} artists &middot; ${topGenre}`.
    New value (use inline styles because Tailwind classes are not available inside Mapbox popup setHTML):
    ```
    const html = `
      <div style="font-weight:600;font-size:13px;margin-bottom:3px;color:#f1f5f9">${props.name}</div>
      <div style="font-size:11px;color:#94a3b8">${props.artist_count} artists &middot; ${topGenre}</div>
    `;
    ```
    This creates a clear visual hierarchy — country name is prominent, metadata is smaller and muted.

    **SearchBar.tsx (UI-05, UI-07):**
    Line 104 — on the input element, add `focus:ring-1 focus:ring-gray-600` to the className string, after the existing `focus:outline-none focus:border-gray-500`. This adds a subtle focus ring consistent with the dark UI theme.

    **Visual consistency audit (UI-01, UI-03, UI-07):**
    Per the research findings, all other text containers already use the correct `min-w-0` + `truncate` pattern. CountryPanel section headers are consistent. AIChatPanel and HomeClient need no changes. No additional modifications required for UI-01, UI-03, or UI-07.
  </action>
  <verify>
    Run `cd /Users/udirno/Desktop/SoundAtlas/frontend && npx tsc --noEmit` — no type errors.
    Grep MapView.tsx for "font-weight:600" — should return a match.
    Grep SearchBar.tsx for "focus:ring-1" — should return a match.
  </verify>
  <done>
    Map tooltip renders country name and metadata on separate styled lines. SearchBar input shows a subtle gray focus ring. All 8 UI requirements (UI-01 through UI-08) are addressed.
  </done>
</task>

</tasks>

<verification>
1. `cd /Users/udirno/Desktop/SoundAtlas/frontend && npx tsc --noEmit` passes with no errors
2. `cd /Users/udirno/Desktop/SoundAtlas/frontend && npm run build` succeeds (Next.js production build)
3. No instances of "letter-spacing-wider" remain in any component file
4. No instances of conflicting text-size classes on h2 elements in StatsSidebar
5. GenrePieChart has a renderLabel function that checks MIN_LABEL_PERCENT
6. CountryPanel top tracks guard includes album_name !== track.name
7. MapView tooltip HTML uses inline-styled divs (not strong/br)
8. SearchBar input has focus:ring classes
</verification>

<success_criteria>
All 8 UI polish requirements satisfied:
- UI-01: Text containment verified (already correct, no changes needed)
- UI-02: StatsSidebar h2 fixed — single text size, valid tracking class
- UI-03: CountryPanel verified (already well-structured, no changes needed)
- UI-04: Pie chart labels suppressed on slices under 5%
- UI-05: SearchBar input has focus ring
- UI-06: Map tooltip has two-line visual hierarchy
- UI-07: Visual consistency confirmed across all panels
- UI-08: Top tracks album_name deduplication guard added
TypeScript compilation passes. Next.js build succeeds.
</success_criteria>

<output>
After completion, create `.planning/phases/07-visual-polish/07-01-SUMMARY.md`
</output>
