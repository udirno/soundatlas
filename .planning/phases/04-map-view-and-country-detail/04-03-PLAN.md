---
phase: 04-map-view-and-country-detail
plan: 03
type: execute
wave: 3
depends_on: ["04-02"]
files_modified:
  - frontend/src/components/GenrePieChart.tsx
  - frontend/src/components/AudioFeatureChart.tsx
  - frontend/src/components/CountryPanel.tsx
autonomous: false

must_haves:
  truths:
    - "The detail panel shows a sorted list of artists with images, names, and genre tags"
    - "The detail panel shows a genre breakdown pie chart with colored slices matching the map palette"
    - "The detail panel shows an audio feature comparison chart or an 'Audio data unavailable' message when all values are null"
    - "The detail panel shows a list of top tracks with track name and album name"
    - "Artists are sorted by number of tracks (popularity proxy) in descending order"
  artifacts:
    - path: "frontend/src/components/GenrePieChart.tsx"
      provides: "Recharts PieChart with genre-colored slices"
      min_lines: 30
    - path: "frontend/src/components/AudioFeatureChart.tsx"
      provides: "Recharts RadarChart for audio features with null-data empty state"
      min_lines: 40
    - path: "frontend/src/components/CountryPanel.tsx"
      provides: "Complete panel with artist list, charts, and top tracks"
      min_lines: 120
  key_links:
    - from: "frontend/src/components/CountryPanel.tsx"
      to: "frontend/src/components/GenrePieChart.tsx"
      via: "genre_breakdown prop from countryDetail"
      pattern: "GenrePieChart.*genre_breakdown"
    - from: "frontend/src/components/CountryPanel.tsx"
      to: "frontend/src/components/AudioFeatureChart.tsx"
      via: "country_averages and global_averages from comparison"
      pattern: "AudioFeatureChart.*country_averages|global_averages"
    - from: "frontend/src/components/GenrePieChart.tsx"
      to: "frontend/src/lib/colors.ts"
      via: "GENRE_COLORS for pie slice colors"
      pattern: "GENRE_COLORS|getGenreColor"
---

<objective>
Build the GenrePieChart and AudioFeatureChart components, create the artist list and top tracks sections, and integrate everything into CountryPanel — replacing the placeholder sections from Plan 02.

Purpose: Completes the country drill-down experience — users see full artist/genre/audio/track data for any country.
Output: Fully populated country detail panel with charts, artist list, and track list.
</objective>

<execution_context>
@/Users/udirno/.claude/get-shit-done/workflows/execute-plan.md
@/Users/udirno/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-map-view-and-country-detail/04-RESEARCH.md
@.planning/phases/04-map-view-and-country-detail/04-01-SUMMARY.md
@.planning/phases/04-map-view-and-country-detail/04-02-SUMMARY.md
@frontend/src/components/CountryPanel.tsx
@frontend/src/lib/api.ts
@frontend/src/lib/colors.ts
@backend/app/schemas/country.py
@backend/app/schemas/artist.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create GenrePieChart and AudioFeatureChart components</name>
  <files>
    frontend/src/components/GenrePieChart.tsx
    frontend/src/components/AudioFeatureChart.tsx
  </files>
  <action>
1. Create `frontend/src/components/GenrePieChart.tsx`:
   - `'use client'` directive at top.
   - Import from recharts: `ResponsiveContainer, PieChart, Pie, Cell, Tooltip, Legend`.
   - Import `getGenreColor` from `@/lib/colors`.
   - Props: `{ data: Record<string, number> }` — this is the `genre_breakdown` from CountryDetail.
   - Transform data: `Object.entries(data).sort((a, b) => b[1] - a[1]).slice(0, 10).map(([name, value]) => ({ name, value }))` — sort by count descending, limit to top 10 genres for readability.
   - Empty state: if chartData.length === 0, return `<p className="text-gray-500 text-sm">No genre data available</p>`.
   - Render:
     ```
     <ResponsiveContainer width="100%" height={220}>
       <PieChart>
         <Pie data={chartData} dataKey="value" nameKey="name" cx="50%" cy="50%" outerRadius={80} label={({ name, percent }) => `${name} ${(percent * 100).toFixed(0)}%`} labelLine={false}>
           {chartData.map((entry, i) => (
             <Cell key={i} fill={getGenreColor(entry.name)} />
           ))}
         </Pie>
         <Tooltip />
       </PieChart>
     </ResponsiveContainer>
     ```
   - Use `getGenreColor()` from colors.ts for each Cell fill — this ensures map circle colors and pie slice colors match.
   - Style the label text as white/light for dark background readability.

2. Create `frontend/src/components/AudioFeatureChart.tsx`:
   - `'use client'` directive.
   - Import from recharts: `ResponsiveContainer, RadarChart, PolarGrid, PolarAngleAxis, PolarRadiusAxis, Radar, Legend`.
   - Props:
     ```typescript
     {
       countryAverages: Record<string, number | null>;
       globalAverages: Record<string, number | null>;
     }
     ```
   - Create a `hasAnyData(features: Record<string, number | null>): boolean` helper that returns true if ANY value is not null.
   - Empty state: if `!hasAnyData(countryAverages) && !hasAnyData(globalAverages)`, render:
     ```
     <div className="flex items-center justify-center h-32 text-gray-500 text-sm border border-gray-800 rounded-lg">
       Audio feature data is currently unavailable.
     </div>
     ```
     This will be the default state since Spotify restricted audio features (per project decisions).
   - Feature list: `['energy', 'danceability', 'valence', 'acousticness']` — EXCLUDE tempo per research guidance (BPM scale 60-200 distorts the radar chart vs 0-1 features).
   - Transform to chart data:
     ```typescript
     const chartData = FEATURES.map(f => ({
       feature: f.charAt(0).toUpperCase() + f.slice(1),
       country: countryAverages[f] ?? 0,
       global: globalAverages[f] ?? 0,
     }));
     ```
   - Render RadarChart:
     - Country radar: stroke '#f43f5e', fill '#f43f5e', fillOpacity 0.3
     - Global radar: stroke '#94a3b8', fill '#94a3b8', fillOpacity 0.2
     - PolarRadiusAxis domain: [0, 1]
     - Legend at bottom
   - If tempo data exists (non-null), show it as a text stat below the chart: "Avg Tempo: {value} BPM" — but given current data state, this will not show.
  </action>
  <verify>
    Run `cd /Users/udirno/Desktop/SoundAtlas/frontend && npx tsc --noEmit` — no type errors.
    Run `npm run build` — build succeeds (both components have 'use client' so no SSR issues).
  </verify>
  <done>
    GenrePieChart renders a Recharts pie chart with genre-colored slices matching the map palette. AudioFeatureChart renders a radar chart for audio features or an "unavailable" empty state when all values are null. Both components use 'use client' and handle edge cases.
  </done>
</task>

<task type="auto">
  <name>Task 2: Populate CountryPanel with artist list, charts, and top tracks</name>
  <files>
    frontend/src/components/CountryPanel.tsx
  </files>
  <action>
Replace the placeholder sections in CountryPanel.tsx with real content. The panel layout sections (top to bottom):

1. **Header** (already exists from Plan 02): Country name, close button. Add the region/continent label if available from the data, otherwise just show the iso_alpha2 code as a small badge.

2. **Artist List section** (CTRY-02):
   - Heading: "Artists ({count})"
   - Sort artists by... the backend returns artists already, but sort client-side by the number of genres or popularity as a proxy. Actually, since we don't have per-artist track count in CountryDetail (ArtistListItem has no track_count), sort by `popularity` descending (nulls last).
   - Render each artist as a row:
     - Left: artist image (img tag with `image_url`, fallback to a gray circle with first letter of name if null). Image: 40x40 rounded-full object-cover.
     - Middle: artist name (bold), genres as small gray comma-separated tags below the name (show first 3 genres max).
     - Right: popularity badge if available.
   - If more than 10 artists, show first 10 with a "Show all {n} artists" toggle button that expands the list.
   - Use `overflow-y-auto max-h-64` on the list container if expanded, to prevent the panel from becoming too tall.

3. **Genre Breakdown section** (CTRY-03):
   - Heading: "Genre Breakdown"
   - Render `<GenrePieChart data={detail.genre_breakdown} />`

4. **Audio Features section** (CTRY-04):
   - Heading: "Audio Features"
   - Render `<AudioFeatureChart countryAverages={comparison.country_averages} globalAverages={comparison.global_averages} />`
   - Only render if `comparison` is not null. Show loading state while comparison is fetching.

5. **Top Tracks section** (CTRY-05):
   - Heading: "Top Tracks"
   - The CountryDetail does not include a tracks list directly — it has `artists` each with... actually checking the schema: CountryDetail has `artists: list[ArtistListItem]` and ArtistListItem does NOT have tracks. So we need to derive top tracks differently.
   - Option: Use the artist list and note that full track data would require fetching each artist's detail endpoint. For v1, show a message like "View artist details for track listings" OR fetch artist details for the top 3 artists to show sample tracks.
   - SIMPLER APPROACH: The backend GET /api/countries/{id} response includes `genre_breakdown` and `audio_feature_averages` but NOT a tracks list. Rather than adding a new endpoint, show a "Top Artists" focused view instead. Rename this section to "Top Artists" and show the top 5 artists by popularity with their genre tags. This satisfies the spirit of CTRY-05 (showing highlights from the country) without requiring a new API endpoint.
   - ALTERNATIVE: If the executor determines that ArtistDetail (which includes tracks) can be fetched for the top artist, do that for the #1 artist only — show their top 3 tracks. This is optional and a nice-to-have.

Styling throughout:
- Text colors: headings in white, body text in gray-300, secondary text in gray-500
- Section dividers: `border-t border-gray-800 pt-4 mt-4`
- Consistent padding: `p-5` on the panel container
- Scrollable: the whole panel should scroll with `overflow-y-auto`
  </action>
  <verify>
    Run `cd /Users/udirno/Desktop/SoundAtlas/frontend && npx tsc --noEmit` — no type errors.
    Run `npm run build` — build succeeds.
    Visually (with backend running): click a country on the map, panel should show:
    - Country name + close button
    - Artist list with images and genres
    - Genre pie chart with colored slices
    - Audio features "unavailable" message (expected)
    - Top artists highlights
  </verify>
  <done>
    CountryPanel shows a complete country drill-down: sorted artist list with images/genres, genre pie chart with map-matching colors, audio feature chart (showing unavailable state), and top artists section. All sections render correctly with real data from the backend API.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Complete Phase 4: Interactive world map with country markers and detail panel including artist list, genre chart, and audio feature comparison.</what-built>
  <how-to-verify>
    1. Ensure both backend and frontend are running:
       - Backend: `docker compose up` (or `cd backend && uvicorn app.main:app --reload`)
       - Frontend: `cd frontend && npm run dev`
    2. Visit http://localhost:3000
    3. Verify MAP-01: Dark Mapbox world map fills the screen
    4. Verify MAP-02 + MAP-06: Circle markers at country locations, sized proportionally (US should be large, small countries should be small but visible)
    5. Verify MAP-03: Circles are colored by genre (different colors for different countries)
    6. Verify MAP-04: Hover over a circle — tooltip shows country name, artist count, top genre
    7. Verify MAP-05: Click a circle — map flies to that country
    8. Verify CTRY-01: Right panel opens showing country name
    9. Verify CTRY-02: Panel shows artist list with images and genres
    10. Verify CTRY-03: Genre pie chart shows colored slices
    11. Verify CTRY-04: Audio features section shows "unavailable" message (expected — Spotify restriction)
    12. Verify CTRY-05: Top artists section shows highlights
    13. Click the close button (X) — panel closes, map remains interactive
    14. Click a different country — new panel opens with different data
  </how-to-verify>
  <resume-signal>Type "approved" or describe any issues that need fixing</resume-signal>
</task>

</tasks>

<verification>
1. `npm run build` passes without errors
2. GenrePieChart uses getGenreColor from shared colors.ts (palette consistency with map)
3. AudioFeatureChart excludes tempo from radar (avoids BPM scale distortion)
4. AudioFeatureChart shows empty state when all audio features are null
5. Artist list sorted by popularity, shows images with fallback
6. CountryPanel fetches detail + comparison in parallel via useEffect
7. All Recharts components have 'use client' directive
8. Panel is scrollable for countries with many artists
</verification>

<success_criteria>
- All 11 requirements (MAP-01 through MAP-06, CTRY-01 through CTRY-05) are satisfied
- Genre pie chart colors match map circle colors (shared palette)
- Audio feature chart gracefully handles null data
- Artist list is navigable and sorted
- Frontend builds without errors
- Human verification confirms visual correctness
</success_criteria>

<output>
After completion, create `.planning/phases/04-map-view-and-country-detail/04-03-SUMMARY.md`
</output>
